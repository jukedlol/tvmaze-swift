platform :ios, '9.0'

target 'TV' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TV
  pod 'Nuke', '~> 7.0'
  pod 'ReSwift', '~> 4.0'
  pod 'Either', :git => "https://github.com/robrix/Either"

  target 'TVTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
